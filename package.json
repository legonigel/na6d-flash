{
  "name": "na6d-flash",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^4.98.0"
  }
}